@model SetLight.Abstracciones.ModelosParaUI.EquipmentDto
@{
    ViewBag.Title = "Registrar nuevo equipo";
}

<div class="d-flex justify-content-center align-items-center min-vh-100 bg-white">
    <div class="form-card p-5 shadow-sm" style="max-width: 720px; width:100%;">
        <h2 class="text-center amb-4 text-uppercase" style="font-weight: 700;">Registrar nuevo equipo</h2>

        @using (Html.BeginForm("CrearEquipment", "Equipment", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "text-danger mb-3" })

            <div class="row">
                <div class="col-md-8">
                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.EquipmentName, new { @class = "form-label" })
                        @Html.TextBoxFor(m => m.EquipmentName, new
                        {
                            @class = "form-control",
                            data_val = "true",
                            data_val_required = "El nombre del equipo es obligatorio.",
                            maxlength = "100",
                            required = "required"
                        })
                        @Html.ValidationMessageFor(m => m.EquipmentName, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.Brand, new { @class = "form-label" })
                        @Html.TextBoxFor(m => m.Brand, new
                        {
                            @class = "form-control",
                            data_val = "true",
                            data_val_required = "La marca es obligatoria.",
                            maxlength = "60",
                            required = "required"
                        })
                        @Html.ValidationMessageFor(m => m.Brand, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.Model, new { @class = "form-label" })
                        @Html.TextBoxFor(m => m.Model, new
                        {
                            @class = "form-control",
                            data_val = "true",
                            data_val_required = "El modelo es obligatorio.",
                            maxlength = "60",
                            required = "required"
                        })
                        @Html.ValidationMessageFor(m => m.Model, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.SerialNumber, new { @class = "form-label" })
                        @Html.TextBoxFor(m => m.SerialNumber, new
                        {
                            @class = "form-control",
                            data_val = "true",
                            data_val_required = "El número de serie es obligatorio.",
                            maxlength = "100",
                            required = "required"
                        })
                        @Html.ValidationMessageFor(m => m.SerialNumber, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.Description, new { @class = "form-label" })
                        @Html.TextAreaFor(m => m.Description, new
                        {
                            @class = "form-control",
                            rows = 3,
                            data_val = "true",
                            data_val_required = "La descripción es obligatoria.",
                            maxlength = "500",
                            required = "required"
                        })
                        @Html.ValidationMessageFor(m => m.Description, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.RentalValue, new { @class = "form-label" })
                        @Html.TextBoxFor(m => m.RentalValue, new
                        {
                            @class = "form-control",
                            type = "number",
                            step = "0.01",
                            min = "0.01",
                            placeholder = "$0.00",
                            data_val = "true",
                            data_val_required = "El valor de alquiler es obligatorio.",
                            data_val_number = "Ingrese un número válido.",
                            data_val_range = "Debe ser mayor a 0.",
                            required = "required"
                        })
                        @Html.ValidationMessageFor(m => m.RentalValue, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.Stock, new { @class = "form-label" })
                        @Html.TextBoxFor(m => m.Stock, new
                        {
                            @class = "form-control",
                            type = "number",
                            step = "1",
                            min = "0",
                            data_val = "true",
                            data_val_required = "El stock es obligatorio.",
                            data_val_number = "Ingrese un número válido.",
                            data_val_range = "No puede ser negativo.",
                            required = "required"
                        })
                        @Html.ValidationMessageFor(m => m.Stock, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-4">
                        @Html.LabelFor(m => m.CategoryId, "Seleccionar categoría", new { @class = "form-label" })
                        <div class="d-flex gap-2">
                            @Html.DropDownListFor(m => m.CategoryId,
                                (IEnumerable<SelectListItem>)ViewBag.Categorias,
                                "-- Seleccione una categoría --",
                                new
                                     {
                                    @class = "form-control",
                                    data_val = "true",
                                    data_val_required = "Debe seleccionar una categoría.",
                                    required = "required"
                                })
                            <button type="button" class="btn btn-outline-danger" data-bs-toggle="modal" data-bs-target="#modalEqCategory">Nueva categoría</button>
                        </div>
                        @Html.ValidationMessageFor(m => m.CategoryId, "", new { @class = "text-danger" })
                    </div>
                </div>

                <!-- Columna imagen (opcional) -->
                <div class="col-md-4">
                    <div class="mb-2">
                        <label class="form-label">Imagen</label>
                        <input type="file" name="imagen" class="form-control" accept=".jpg,.jpeg,.png,.webp" />
                        <small class="text-muted">Formatos: JPG, JPEG, PNG, WEBP. Máx 5MB.</small>
                    </div>
                    <div class="border rounded p-2 text-center">
                        <img id="previewCrear" src="@Url.Content("~/content/img/placeholder-equipment.png")"
                             alt="preview" style="width:100%; max-height:220px; object-fit:cover; border-radius:8px;" />
                    </div>
                </div>
            </div>

            <div class="form-group text-center mt-4">
                <input type="submit" value="Guardar equipo" class="btn btn-dark me-2" />
                @Html.ActionLink("Volver", "ListarEquipment", null, new { @class = "btn btn-outline-dark" })
            </div>
        }
    </div>
</div>

<div class="modal fade" id="modalEqCategory" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content" id="modal-content-category"></div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/jqueryunobtrusive")
    <script src="~/Scripts/Modals/EqCategory/EqCategoryModal.js"></script>

    <script>
        $(document).on('change', 'input[name="imagen"]', function () {
            const file = this.files && this.files[0];
            if (!file) return;
            const reader = new FileReader();
            reader.onload = function (ev) {
                $('#previewCrear').attr('src', ev.target.result);
            };
            reader.readAsDataURL(file);
        });
    </script>
}
