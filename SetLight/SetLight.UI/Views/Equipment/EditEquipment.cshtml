@model SetLight.Abstracciones.ModelosParaUI.EquipmentDto
@{
    ViewBag.Title = "Editar equipo";

    // Helper para mostrar URL absoluta o virtual, y fallback a placeholder
    Func<string, string> Src = (url) =>
    {
        if (string.IsNullOrWhiteSpace(url)) return Url.Content("~/content/img/placeholder-equipment.png");
        return url.StartsWith("http", StringComparison.OrdinalIgnoreCase) ? url : Url.Content(url);
    };
}

<div class="d-flex justify-content-center align-items-center min-vh-100 bg-white">
    <div class="form-card p-5 shadow-sm" style="max-width: 720px; width:100%;">
        <h2 class="text-center amb-4 text-uppercase" style="font-weight: 700;">Editar equipo</h2>

        @using (Html.BeginForm("Edit", "Equipment", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.EquipmentId)
            @Html.ValidationSummary(true, "", new { @class = "text-danger mb-3" })

            <div class="row">
                <div class="col-md-8">
                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.EquipmentName, new { @class = "form-label" })
                        @Html.EditorFor(m => m.EquipmentName, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(m => m.EquipmentName, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.Brand, new { @class = "form-label" })
                        @Html.EditorFor(m => m.Brand, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(m => m.Brand, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.Model, new { @class = "form-label" })
                        @Html.EditorFor(m => m.Model, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(m => m.Model, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.SerialNumber, new { @class = "form-label" })
                        @Html.EditorFor(m => m.SerialNumber, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(m => m.SerialNumber, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.Description, new { @class = "form-label" })
                        @Html.TextAreaFor(m => m.Description, new { @class = "form-control", rows = 3 })
                        @Html.ValidationMessageFor(m => m.Description, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.RentalValue, new { @class = "form-label" })
                        @Html.EditorFor(m => m.RentalValue, new { htmlAttributes = new { @class = "form-control", placeholder = "$0.00" } })
                        @Html.ValidationMessageFor(m => m.RentalValue, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.Stock, new { @class = "form-label" })
                        @Html.EditorFor(m => m.Stock, new { htmlAttributes = new { @class = "form-control", type = "number", min = "0" } })
                        @Html.ValidationMessageFor(m => m.Stock, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-4">
                        @Html.LabelFor(m => m.CategoryId, "Seleccionar categoría", new { @class = "form-label" })
                        <div class="d-flex gap-2">
                            @Html.DropDownListFor(m => m.CategoryId, (IEnumerable<SelectListItem>)ViewBag.Categorias, "-- Seleccione una categoría --", new { @class = "form-control" })
                            <button type="button" class="btn btn-outline-danger" data-bs-toggle="modal" data-bs-target="#modalEqCategory">Nueva categoría</button>
                        </div>
                        @Html.ValidationMessageFor(m => m.CategoryId, "", new { @class = "text-danger" })
                    </div>
                </div>

                <!-- Columna imagen -->
                <div class="col-md-4">
                    <div class="border rounded p-2 text-center mb-2">
                        <img id="previewEdit" src="@Src(Model.ImageUrl)"
                             alt="imagen actual" style="width:100%; max-height:220px; object-fit:cover; border-radius:8px;" />
                    </div>

                    <div class="mb-2">
                        <label class="form-label">Reemplazar imagen</label>
                        <!-- Debe llamarse 'nuevaImagen' para matchear con el controller -->
                        <input type="file" name="nuevaImagen" class="form-control" accept=".jpg,.jpeg,.png,.webp" />
                        <small class="text-muted">Si seleccionas una nueva imagen, reemplazará a la actual.</small>
                    </div>

                    <div class="form-check my-2">
                        <input class="form-check-input" type="checkbox" name="eliminarImagen" id="eliminarImagen" />
                        <label class="form-check-label" for="eliminarImagen">Eliminar imagen actual</label>
                    </div>
                </div>
            </div>

            <div class="form-group text-center mt-4">
                <input type="submit" value="Guardar" class="btn btn-dark me-2" />
                @Html.ActionLink("Volver", "ListarEquipment", null, new { @class = "btn btn-outline-dark" })
            </div>
        }
    </div>
</div>

<div class="modal fade" id="modalEqCategory" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content" id="modal-content-category"></div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/jqueryunobtrusive")

    <script>
        // Preview en Edit al elegir nueva imagen
        $(document).on('change', 'input[name="nuevaImagen"]', function () {
            const file = this.files && this.files[0];
            if (!file) return;
            const reader = new FileReader();
            reader.onload = function (ev) {
                $('#previewEdit').attr('src', ev.target.result);
                $('#eliminarImagen').prop('checked', false); // si sube nueva, desmarca eliminar
            };
            reader.readAsDataURL(file);
        });

        // Si marcan eliminar, vuelve al placeholder y limpia file input
        $(document).on('change', '#eliminarImagen', function () {
            if (this.checked) {
                $('#previewEdit').attr('src', '@Url.Content("~/content/img/placeholder-equipment.png")');
                $('input[name="nuevaImagen"]').val('');
            }
        });
    </script>
}
